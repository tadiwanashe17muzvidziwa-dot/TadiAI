# Copy this file to .env and add your real API key
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash
FLASK_DEBUG=1
PORT=5000

# CORS — comma-separated origins (use * for dev, restrict in production)
CORS_ORIGINS=*
